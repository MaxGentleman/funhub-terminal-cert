node_modules/
dist/
.vercel/
.env
.env.*
!.env.example
*.log
.DS_Store

# Never commit the Google service-account key. It lives in Supabase secrets.
*service-account*.json
*credentials*.json

# vite writes these while resolving the config; they are never wanted
vite.config.js.timestamp-*.mjs
_to_delete/
_REFUND SHEETS PDF/
